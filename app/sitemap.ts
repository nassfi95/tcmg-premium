import type { MetadataRoute } from 'next'; const paths=['','le-club','ecole-de-tennis','adultes','competition','fete-le-mur','actualites','galerie','boutique','contact','partenaires','reserver-un-terrain','espace-licencie','admin']; export default function sitemap():MetadataRoute.Sitemap{return paths.map(path=>({url:`https://tcmg.fr/${path}`,lastModified:new Date(),changeFrequency:'weekly',priority:path===''?1:.7}));}
