'use client';
import { AnimatePresence, motion } from 'framer-motion'; import { useEffect, useState } from 'react';
export function LoadingScreen(){const [visible,setVisible]=useState(true);useEffect(()=>{const t=setTimeout(()=>setVisible(false),900);return()=>clearTimeout(t)},[]);return <AnimatePresence>{visible&&<motion.div initial={{opacity:1}} exit={{opacity:0,transition:{duration:.45}}} className="fixed inset-0 z-[100] grid place-items-center bg-[#071120]"><motion.div initial={{scale:.7,opacity:0}} animate={{scale:1,opacity:1}} transition={{type:'spring',stiffness:190,damping:16}} className="text-center"><img src="/tcmg-mark.svg" alt="" className="mx-auto h-20 w-20"/><p className="mt-5 text-[10px] font-bold tracking-[.3em] text-white/70">LE JEU COMMENCE</p></motion.div></motion.div>}</AnimatePresence>}
